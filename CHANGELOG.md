# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，发布日期以 GitHub Release 为准。

## Unreleased

- 在 B 站设置页顶部新增“哔哩空降助手”入口；普通安装时打开模块完整设置页，LSPatch 仅内嵌模块时提供可持久化的内嵌设置面板，并与独立模块设置双向同步。
- 从当前版本开始，会在 tg 频道不定时发布（主要是重要功能更新后）内嵌模块的 B 站安装包，频道见模块设置的“关于“处

## 1.3.0

- 将片段分类的简单开关升级为“自动跳过”“手动跳过”“在进度条中显示”和“禁用”四种可选行为，默认选项与原版 BilibiliSponsorBlock 浏览器插件保持一致。
- 新增手动跳过交互：进入相应片段时，在播放器左下角显示带“跳过”按钮的原生气泡通知（原本是给广告用的）。
- 自动迁移旧版分类开关与全局自动跳过设置，升级后保留原有跳过行为。
- 更新应用图标，并在关于页面新增 Telegram 频道入口。
- 适配并真机验证粉版哔哩哔哩 9.8.0 与白版哔哩哔哩 3.20.4（Play最新版暂未主动测试，如有需求请提 issue），分类行为和播放器交互通知均测试通过。

## 1.2.2

- 适配了32 位粉版8.75 和9.4.0版
- 修复了已跳过片段和时间记录失败的问题

## 1.2.1

- 片段开始、片段结束、取消和提交按钮新增点击震动反馈，并在刷新片段、投票和提交期间显示转圈加载动画。
- 模块设置页迁移到 Material 3 与 AndroidX Preference，支持日夜主题和 Android 12 及以上系统动态配色；优化选择类和输入类设置项的对齐与操作标识。
- 设置页新增数据展示，显示用户提交片段为社区贡献的跳过量和节省时间，并在每次成功自动跳过后持久累计本机跳过片段数与实际节省时长；统计数字使用粗体突出显示，支持通过标题栏刷新按钮重新获取数据。

## 1.2.0

- 新增播放器控制栏“片段提交与投票”入口，可在设置中独立控制显示。
- 新增片段提交菜单：依次记录片段开始与结束时间、选择片段类别，并在数据完整后通过 API 提交；支持随时取消当前草稿。
- 新增片段投票菜单：展示当前分 P 的全部片段、类别色点、起止时间和票数，支持使用图标进行赞同或反对投票，并可手动刷新最新数据。
- 设置页新增公开用户名和私有用户 ID 管理；私有 ID 默认在本机随机生成，公开用户名可同步到服务端。

## 1.1.0

- 新增详情页、竖屏播放器下方和横屏播放器上方的视频标题分类标签，颜色与网页端“小电视空降助手”保持一致。
- 新增普通播放器与 Story 播放器竖屏、横屏进度条的特殊片段色块。
- 播放器动态解析增加 `getDuration()`，用于按视频总时长计算片段位置。
- 新增标题标签和进度条标记设置开关。
- 修复活动视频详情页将特殊片段标签错误注入活动入口，并确保原有“活动”徽标、特殊片段标签与标题依次排列且不重叠。
- 详情页标签改为跟随折叠/展开文本重建的行内绘制，修复折叠简介时被裁切及展开后垂直错位的问题。
- 监听播放器页面布局变化，在进入/退出全屏和标题控件切换时立即刷新标签与进度标记。
- 适配并真机验证粉版哔哩哔哩 9.5.0。
- 设置页新增关于信息，可查看当前版本、访问作者 GitHub 或通过邮件联系。

## 0.2.4

- 修复 LSPosed 无法从压缩 APK 加载 `libdexkit.so`，native library 改为未压缩并按 16KiB 页面对齐。
- 经粉版哔哩哔哩 9.4.0 真机验证，播放器定位、进度监听和自动跳过均正常工作。
- 新增写入 B 站外部文件目录的持久诊断日志。

## 0.2.2

- 新增 Hook、播放器实例、播放进度和 seek 四阶段失败诊断 Toast。

## 0.2.1

- 根据稳定字符串动态定位播放器状态方法，并加入主动播放进度轮询。
- 设置读取改用 `XSharedPreferences`，兼容 MIUI 应用可见性限制。
- 接入应用图标。

## 0.2.0

- 新增模块设置入口、发现片段 Toast 和跳过结果 Toast。
- 新增最短片段时长、“快进到片段中间时仍跳过”和九种片段分类设置。

## 0.1.0

- 首个可构建版本：获取 BVID/CID、请求特殊片段数据并尝试通过播放器 Hook 自动跳过。
